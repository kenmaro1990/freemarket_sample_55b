%html
  %head
  %body
    =render partial: "shared/header"

    .mypage__wrapper
      .mypage__identification









      
    %h2 クレジットカード情報
      .customer-card__content
        %figure.form_space
        .customer-card__content--number.form_space
          %h3
            = "**** **** **** " + @customer_card.last4
          .customer-card__content--expired.form_space
            - exp_month = @customer_card.exp_month.to_s
            - exp_year = @customer_card.exp_year.to_s.slice(2,3)
            %h3
              = exp_month + " / " + exp_year
          = link_to delete_card_index_path ,method: :delete, class:"customer-card__content--btn" do
            %button{type: "submit",class: "btn-red"}
              削除する
  = render partial:"users/sidebar"
  = render partial: "shared/exhibit"
  = render partial: "shared/footer"
